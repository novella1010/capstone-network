Need to adjust the Server access management database.

POS data to Square with inventory storage.

Maybe show switch configuration as well

Go in depth in port security and port monitoring SNMP *TRAPS* Open source container server LIMUX Based Ubuntu Grafana (visual).

All this behind PAM Management server raid array. Data management AWS S3 Buckets? Cloud.

Go in depth with infrastructure. 
